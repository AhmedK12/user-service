server.port=9090
spring.application.name=user-service
# Set the default password encoder
security.password.encoder=argon2

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/apica_user_journal
spring.datasource.username=root
spring.datasource.password=kali
spring.jpa.hibernate.ddl-auto=update

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings (HikariCP is default)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000


jwt.secret=A67$%#!@WEv8y!2ds@89LXzqwERt12#z  # Must be at least 32 characters!
jwt.algorithm=HS256
jwt.privateKey=kl
jwt.publicKey=lk
security.jwt.refresh.expiry=12000

logging.level.org.springframework.security=DEBUG


//KAFKA
# Kafka Bootstrap server(s)
kafka.bootstrap-servers=localhost:9092

# Kafka topic for user activity logs
app.kafka.topic.user-activity=user-events

# Producer configurations (optional but good practice)
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer


# Enable/disable user activity tracking
user.activity.tracking.enabled=true
auto.create.topics.enable=true

